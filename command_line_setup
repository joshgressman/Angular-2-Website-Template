1. npm init (enter all the way through)

2. take the json from the project folder and paste into the package.json

3. Add the tsconfig.json

4. Add the typing.json

5. add the systemjs.config.js

6. npm install

7. Add (create a folder called app) app.module.ts
